const SVG_NS='http://www.w3.org/2000/svg';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const uid=()=>Math.random().toString(36).slice(2,9);
let installed=false;
function button(label,id){const b=document.createElement('button');b.className='btn';b.id=id;b.textContent=label;return b;}
function enhance(){
 if(installed||!document.querySelector('#tbBoard'))return;installed=true;
 const board=document.querySelector('#tbBoard'),viewport=document.querySelector('#tbViewport'),toolbar=document.querySelector('#tbToolbar'),top=document.querySelector('.tactical-topline');if(!board||!viewport)return;
 const extra=document.createElement('div');extra.className='tb-enhanced-tools';
 const measure=button('⌗ Medir','tbMeasure'),grid=button('▦ Grade','tbGrid'),fit=button('⛶ Enquadrar','tbFit'),snapshot=button('◎ Snapshot','tbSnapshot'),clear=button('◉ Visuais','tbClearLayer');[measure,grid,fit,snapshot,clear].forEach(x=>extra.appendChild(x));toolbar?.appendChild(extra);
 const timeline=document.createElement('div');timeline.className='tb-timeline';timeline.innerHTML='<div class="eyebrow">LINHA DE EXECUÇÃO</div><div class="tb-timeline-row"><input id="tbTimeline" type="range" min="0" max="1800" value="0" step="15"><output id="tbTimelineOut">00:00</output><button class="btn" id="tbTimelineAdd">＋ Marco</button></div><div id="tbTimelineMarks" class="tb-timeline-marks"></div>';document.querySelector('.tb-board-card')?.appendChild(timeline);
 const input=timeline.querySelector('#tbTimeline'),output=timeline.querySelector('#tbTimelineOut'),marks=timeline.querySelector('#tbTimelineMarks');const state={grid:false,measure:false,points:[],markers:[],visualsHidden:false};
 const fmt=s=>{const m=Math.floor(s/60),sec=String(s%60).padStart(2,'0');return `${String(m).padStart(2,'0')}:${sec}`;};input.oninput=()=>output.value=fmt(+input.value);
 timeline.querySelector('#tbTimelineAdd').onclick=()=>{const time=+input.value,tag=prompt('Nome do marco tático:',time?`Objetivo ${fmt(time)}`:'Abertura');if(!tag)return;state.markers.push({id:uid(),time,tag});renderMarks();};
 function renderMarks(){marks.innerHTML=state.markers.sort((a,b)=>a.time-b.time).map(m=>`<button class="tb-time-mark" data-time="${m.time}"><b>${fmt(m.time)}</b> ${esc(m.tag)}</button>`).join('');marks.querySelectorAll('[data-time]').forEach(b=>b.onclick=()=>{input.value=b.dataset.time;input.dispatchEvent(new Event('input'));});}
 grid.onclick=()=>{state.grid=!state.grid;grid.classList.toggle('active',state.grid);let g=board.querySelector('#tbEnhGrid');if(state.grid&&!g){g=document.createElementNS(SVG_NS,'g');g.id='tbEnhGrid';for(let i=50;i<1000;i+=50){const v=document.createElementNS(SVG_NS,'line');v.setAttribute('x1',i);v.setAttribute('x2',i);v.setAttribute('y1',0);v.setAttribute('y2',1000);g.appendChild(v);const h=document.createElementNS(SVG_NS,'line');h.setAttribute('x1',0);h.setAttribute('x2',1000);h.setAttribute('y1',i);h.setAttribute('y2',i);g.appendChild(h);}board.appendChild(g);}if(g)g.style.display=state.grid?'block':'none';};
 fit.onclick=()=>{viewport.scrollTo({left:0,top:0,behavior:'smooth'});const stage=document.querySelector('#tbStage');if(stage){stage.style.transform='scale(1)';stage.style.transformOrigin='top left';}};
 measure.onclick=()=>{state.measure=!state.measure;measure.classList.toggle('active',state.measure);measure.textContent=state.measure?'⌗ Medindo…':'⌗ Medir';state.points=[];};
 board.addEventListener('click',e=>{if(!state.measure)return;const r=board.getBoundingClientRect(),p={x:(e.clientX-r.left)/r.width*1000,y:(e.clientY-r.top)/r.height*1000};state.points.push(p);if(state.points.length<2)return;const a=state.points[0],b=state.points[1],d=Math.round(Math.hypot(b.x-a.x,b.y-a.y));let layer=document.querySelector('#tbMeasureLayer');if(!layer){layer=document.createElementNS(SVG_NS,'g');layer.id='tbMeasureLayer';board.appendChild(layer);}layer.innerHTML=`<line x1="${a.x}" y1="${a.y}" x2="${b.x}" y2="${b.y}" stroke="#fff" stroke-width="4" stroke-dasharray="12 8"/><circle cx="${a.x}" cy="${a.y}" r="8" fill="#fff"/><circle cx="${b.x}" cy="${b.y}" r="8" fill="#fff"/><text x="${(a.x+b.x)/2}" y="${(a.y+b.y)/2-12}" fill="#fff" font-size="18" font-weight="900" text-anchor="middle">${d}u</text>`;state.points=[];});
 clear.onclick=()=>{state.visualsHidden=!state.visualsHidden;clear.classList.toggle('active',state.visualsHidden);['tbRoutes','tbZones','tbObjectives','tbVision','tbChampions','tbNotes','tbMeasureLayer'].forEach(id=>{const el=document.getElementById(id);if(el)el.style.opacity=state.visualsHidden?'0':'1';});clear.textContent=state.visualsHidden?'◉ Mostrar':'◉ Visuais';};
 snapshot.onclick=()=>{const copy=board.cloneNode(true);copy.querySelectorAll('#tbTemp,#tbMeasureLayer,#tbEnhGrid').forEach(x=>x.remove());const xml=new XMLSerializer().serializeToString(copy),blob=new Blob([xml],{type:'image/svg+xml'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`frombos-tactical-${Date.now()}.svg`;a.click();setTimeout(()=>URL.revokeObjectURL(url),500);};
 if(top){const badge=document.createElement('span');badge.className='badge cyan';badge.textContent='TACTICAL PRO';top.querySelector('.scenario-control')?.appendChild(badge);}
}
new MutationObserver(enhance).observe(document.body,{childList:true,subtree:true});enhance();